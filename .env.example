# Secret used to authorize POST /api/revalidate. Set this to a long random value.
REVALIDATE_TOKEN=change-me

# Optional: absolute path to the content root. Defaults to <cwd>/content.
# CONTENT_DIR=/app/content
